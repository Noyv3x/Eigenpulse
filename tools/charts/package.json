{
  "name": "@eigenpulse/charts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:bundle": "node scripts/build.mjs --check",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "echarts": "6.1.0"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
