{
  "name": "Eigenpulse · 记一笔",
  "icon_glyph": "creditcard.fill",
  "icon_tint": "amber",
  "description": "Prompt for amount/merchant/category, POST to /api/v1/fin/txn, show the new doc id.",
  "endpoint": {
    "method": "POST",
    "url": "{EP_BASE}/api/v1/fin/txn",
    "scope_required": "fin:write",
    "headers": {
      "Authorization": "Bearer {EP_TOKEN}",
      "Content-Type": "application/json"
    }
  },
  "config": {
    "EP_BASE":  "http://192.168.1.50:3000",
    "EP_TOKEN": "ep_pat_REPLACE_ME"
  },
  "steps": [
    {
      "n": 1,
      "action": "Comment",
      "text": "Set your instance URL, PAT, and your own finance account/category codes. Fresh installs do not ship demo accounts/categories."
    },
    {
      "n": 2,
      "action": "Text",
      "bind": "EP_BASE",
      "value": "http://192.168.1.50:3000"
    },
    {
      "n": 3,
      "action": "Text",
      "bind": "EP_TOKEN",
      "value": "ep_pat_REPLACE_ME"
    },
    {
      "n": 4,
      "action": "Ask for Input",
      "bind": "Amount",
      "input_type": "Number",
      "prompt": "金额 (¥) — 支出留正数，下一步选支出/收入",
      "default": "0"
    },
    {
      "n": 5,
      "action": "Ask for Input",
      "bind": "Merchant",
      "input_type": "Text",
      "prompt": "商户 / 摘要"
    },
    {
      "n": 6,
      "action": "Choose from Menu",
      "bind": "Tag",
      "prompt": "类型",
      "items": [
        { "label": "支出 (exp)",  "value": "exp" },
        { "label": "收入 (inc)",  "value": "inc" }
      ]
    },
    {
      "n": 7,
      "action": "Ask for Input",
      "bind": "Category",
      "input_type": "Text",
      "prompt": "类别代码 (使用你在 Finance 中创建的 fin_category.code)"
    },
    {
      "n": 8,
      "action": "Ask for Input",
      "bind": "Account",
      "input_type": "Text",
      "prompt": "账户代码 (使用你在 Finance 中创建的 fin_account.code)"
    },
    {
      "n": 9,
      "action": "Calculate",
      "bind": "SignedAmount",
      "expression": "if Tag == 'exp' then -Amount else Amount",
      "note": "Shortcuts has no inline ternary; recreate this with: Get Variable Tag → If 'is exp' → Calculate Amount × -1 → Otherwise → Amount."
    },
    {
      "n": 10,
      "action": "Dictionary",
      "bind": "Body",
      "value": {
        "merchant":      "{{Merchant}}",
        "category_code": "{{Category}}",
        "account_code":  "{{Account}}",
        "amount":        "{{SignedAmount}}",
        "tag":           "{{Tag}}"
      }
    },
    {
      "n": 11,
      "action": "Get Contents of URL",
      "bind": "Response",
      "method": "POST",
      "url": "{{EP_BASE}}/api/v1/fin/txn",
      "headers": {
        "Authorization": "Bearer {{EP_TOKEN}}",
        "Content-Type":  "application/json"
      },
      "request_body": "{{Body}}"
    },
    {
      "n": 12,
      "action": "Get Dictionary Value",
      "bind": "DocId",
      "from": "{{Response}}",
      "key": "doc_id"
    },
    {
      "n": 13,
      "action": "Show Notification",
      "title": "已记账 · {{DocId}}",
      "body":  "{{Merchant}} · ¥{{SignedAmount}}"
    }
  ],
  "request_body_schema": {
    "merchant":      "string, required",
    "category_code": "string, existing fin_category.code",
    "account_code":  "string, existing fin_account.code",
    "amount":        "number; negative = expense, positive = income",
    "tag":           "string, one of exp/inc; use /api/v1/fin/transfer for transfers",
    "note":          "string, optional",
    "linked_doc_id": "string, optional (e.g. FIT-S-0412 to cross-link)",
    "occurred_at":   "integer unix seconds, optional (defaults to server now)"
  },
  "response_shape": { "doc_id": "FIN-26092" },
  "side_effects": [
    "Inserts a fin_txn row.",
    "Adjusts the chosen account's balance (atomic with the insert).",
    "Writes an activity row visible on Dashboard / Today.",
    "If amount < -500.0, dispatches a Warn-level notification via NotifyBus."
  ]
}
