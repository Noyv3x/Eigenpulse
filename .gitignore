/target
/data
# Hand-rolled DB snapshots (e.g. `cp -a data data.bak.$(date +%H%M%S)`).
/data.bak.*/
**/*.rs.bk
.DS_Store
node_modules
secret.key
.env
.env.local
.idea/
.vscode/
.claude/
.playwright-mcp/
# Playwright screenshots dumped at the repo root via browser_take_screenshot
# (default filename = `page-{ts}.png`; ad-hoc filenames also land here).
/*.png
/*.jpg
/*.jpeg
/.antigravitycli/
